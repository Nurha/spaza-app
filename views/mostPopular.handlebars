<h1>Most Popular Product</h1>
<table class>
	<thead>
		<th>Product Name</th>
		<th>Amount</th>
	</thead>
	{{#product}}
		<tr>
			<td>{{prodName}}</td>
			<td>{{amount}}</td>
		</tr>
	{{/product}}
                  
</table>

<!-- {{#each product}}
	<h2>{{@key}} - {{this}}</h2>

{{/each}}
 -->