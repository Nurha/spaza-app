<h1>User List</h1>

<div class = "container-fluid">
	<div class = "row">
		<div class = "col-xs-12">
      <div class = "table-responsive">
				<table class = "table" id = "userList">
					<thead>
						<tr>
							<th>User Name</th>
							<th>User Role</th>
              <th>User Status</th>
						</tr>
					</thead>
					{{#user}}
						<tr>
							<td>{{user_name}}</td>
							<td>{{description}}</td>
							<td>{{locked}}</td>

              <!-- <td><a href="/user/editUser/{{user_id}}" class="btn btn-primary">
							<span class="glyphicon glyphicon-edit"></span> Edit</a></td> -->

            <!--  <td><a href="/useer/delete/{{user_id}}" class= "btn btn-primary">
							<span class="glyphicon glyphicon-trash"></span> Delete</a></td> -->
						</tr>
					{{/user}}
				</table>
			</div>
		</div>
	</div>
</div>
